/** The width of the one mip a preview requests per texture, in pixels. */
export const PREVIEW_MIP_WIDTH = 128;

/** The ground point every preview camera frames about. */
export const PREVIEW_GROUND = [0, 0, 0] as const;
