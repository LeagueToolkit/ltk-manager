
### Added

- browse game sources as tiles and rows
- support pinned tabs
- support group locking
- upload release source maps
- report backend errors, panics and UI crashes
- gate collection on a published configuration
- arrange shell panes as a split tree
- size and filter the emitter strip
- report game session outcomes
- read a curve as keys and probability tables
- draw colour curves as gradient stops
- add diagnostics setting and first-run notice
- draw rows and toolbars for narrow panes
- add diagnostics transport crate
- enable the React Compiler
- report unhandled mutation failures
- draw a particle system in panes
- draw emitters as component cards
- read whether a value animates
- skin and particle system layouts
- name a project's own chunks
- view mode and material layout
- projected read and value rows
- link a string that names a thing
- bin editor ux improvements

### Documentation

- disclose what anonymous diagnostics send
- record what the audit has landed
- name the Tauri event hooks
- add frontend architecture audit
- decide the particle system shell
- sync the bin editor status
- propose the curve panel
- decide the class views

### Fixed

- rewrite the diagnostics notice for a first-time reader
- acknowledge spool deliveries by mark
- workshop shell styling
- mark a chunk nothing holds
- scale class view and mount its contexts

### Other

- update
- format the privacy card and page constant
- record ltk-telemetry
- tighter border radius
- publish patch 16.17 notice
- adopt eslint-plugin-query recommended
- scope the barrel rule to cross-module imports
- run tsgo
- replace prettier with oxfmt
- move the cycle guard to oxlint
- drop baseUrl and widen lib to ES2022
- break the sub-barrel import cycles
- break the workshop overview self-barrel import
- guard module cycles and barrel imports
- drop DataTable, react-table and query devtools

### Performance

- slim root route chunk
- window the mod grid
- answer card thumbnails in one call
- drive status from a phase event
- lazy the mod card thumbnail
- load selectable faces on demand

### Refactored

- move published configuration to config
- split tree hooks and re-measure rows
- group mutation options into namespaced factories
- group query options into namespaced factories
- lift shared frame and confirm body
- share the facet filter slice
- move single-consumer stores into modules
- move path helpers onto dir types
- make the module graph a DAG
- lift library-coupled hooks out of patcher
- own the editor store
- own the auto-start hook
- own the HDD warning hook
- move ToastType to a leaf module
- funnel reveal in explorer through revealPath
- fold inline query keys into factories
- export path from the barrel
- name section messages by role

